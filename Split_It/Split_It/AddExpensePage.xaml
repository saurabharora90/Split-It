<Page
    x:Class="Split_It.AddExpensePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Split_It"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.DataContext>
        <Binding Path="AddExpenseVM" Source="{StaticResource Locator}"/>
    </Page.DataContext>

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Skins/AddExpenseSkin.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Background="{StaticResource splitwiseGreen}" Orientation="Horizontal">
            <Button FontFamily="Segoe MDL2 Assets"
					Foreground="White"
					Content="&#xE72B;"
					Width="50" Height="50" 
                    Command="{Binding GoBackCommand}"
					Background="Transparent" Style="{StaticResource NoBorderSelectButton}"/>

            <TextBlock Text="Add Expense" Foreground="White" VerticalAlignment="Center" Margin="10,0,0,0"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" Padding='20,15'>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center">
                        <Run Text="With"/>
                        <Run Text="you" FontWeight="Bold"/>
                        <Run Text="and:"/>
                    </TextBlock>
                    <AutoSuggestBox Grid.Column="1"/>
                </Grid>

                <Grid Grid.Row="3">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="60"/>
                        <RowDefinition Height="60"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Text="Date:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <DatePicker Grid.Column="1" MonthFormat="{}{month.abbreviated}" 
                                Date="{Binding ExpenseToAdd.CreatedAt, Mode=TwoWay}" />
                    <TextBlock Text="Part of Group:" VerticalAlignment="Center" Margin="0,0,5,0" Grid.Row="1"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" VerticalAlignment="Center"/>
                    
                </Grid>

                <StackPanel Orientation="Vertical" Grid.Row="4" Margin="0,15">
                    <TextBlock Text="Additional Notes:" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding ExpenseToAdd.Details}" AcceptsReturn="True" 
                                 Padding="0"
                                 TextWrapping="Wrap" InputScope="Text"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>

    <Page.BottomAppBar>
        <CommandBar ClosedDisplayMode="Minimal">
            <AppBarButton Command="{Binding RecordExpenseCommand}" Icon="Save" Label="save"/>
        </CommandBar>
    </Page.BottomAppBar>
</Page>
